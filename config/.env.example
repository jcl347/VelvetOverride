# LinkedIn credentials.
# If you sign in with Google/SSO, you can LEAVE THE PASSWORD BLANK — on the
# first run the bot opens Chrome and waits for you to sign in by hand, then
# persists the session (browser_data/) so later runs log in automatically.
LINKEDIN_EMAIL=your_email@example.com
LINKEDIN_PASSWORD=

# ── LLM provider ──
# OpenAI (ChatGPT) is the default provider. Optional if you use
# resume.mode: static and fit.enabled: false (then no LLM is needed).
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Anthropic Claude (only used if llm.provider: anthropic in settings.yaml)
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: CAPTCHA solving API key (2Captcha or CapSolver)
# CAPTCHA_API_KEY=your_captcha_api_key_here

# Optional: proxy configuration
# PROXY_URL=http://user:pass@proxy-host:port
